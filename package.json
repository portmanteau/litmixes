{
  "name": "litmix",
  "private": true,
  "scripts": {
    "start": "meteor --settings settings.json",
    "push-config": "heroku config:set METEOR_SETTINGS=\"$(cat settings.production.json)\""
  },
  "dependencies": {
    "meteor-node-stubs": "^0.2.3",
    "xhr2": "^0.1.3",
    "jsmediatags": "^3.0.5"
  },
  "engines": {
    "node": "4.1.1"
  }
}
